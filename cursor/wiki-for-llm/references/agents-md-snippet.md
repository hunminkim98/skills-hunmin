# AGENTS.md snippet

Add this only when the repo has no richer wiki closeout yet. Keep it to a pointer so the skill, not AGENTS.md, holds the procedure.

```markdown
## Work record

After major work that changes code, docs, architecture, workflow, Git state, or durable decisions, follow the `wiki-for-llm` skill before the final reply. Do not wait for the user to ask. Skip read-only questions, typos, and exploration that leave no durable state.
```
